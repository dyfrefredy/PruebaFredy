import { AvDateTimeOffsetPipe } from './av-date-time-offset.pipe';

describe('AvDateTimeOffsetPipe', () => {
  it('create an instance', () => {
    const pipe = new AvDateTimeOffsetPipe();
    expect(pipe).toBeTruthy();
  });
});
