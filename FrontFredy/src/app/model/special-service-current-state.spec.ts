import { SpecialServiceCurrentState } from './special-service-current-state';

describe('SpecialServiceCurrentState', () => {
  it('should create an instance', () => {
    expect(new SpecialServiceCurrentState()).toBeTruthy();
  });
});
