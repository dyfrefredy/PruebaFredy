import { ShipmentDetailsFilter } from './shipment-details-filter.model';

describe('ShipmentDetailsFilter', () => {
  it('should create an instance', () => {
    expect(new ShipmentDetailsFilter()).toBeTruthy();
  });
});
