import { SpecialServiceRouteStatus } from './special-service-route-status';

describe('SpecialServiceRouteStatus', () => {
  it('should create an instance', () => {
    expect(new SpecialServiceRouteStatus()).toBeTruthy();
  });
});
