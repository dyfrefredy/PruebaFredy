import { AirWaybillTemperatureChart } from './air-waybill-temperature-chart';

describe('AirWaybillTemperatureChart', () => {
  it('should create an instance', () => {
    expect(new AirWaybillTemperatureChart()).toBeTruthy();
  });
});
