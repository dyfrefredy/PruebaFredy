import { ConfigurationsTable } from './configurations-table';

describe('ConfigurationsTable', () => {
  it('should create an instance', () => {
    expect(new ConfigurationsTable()).toBeTruthy();
  });
});
