import { SpecialServiceRoute } from './special-service-route';

describe('ServiceRoute', () => {
  it('should create an instance', () => {
    expect(new SpecialServiceRoute()).toBeTruthy();
  });
});
