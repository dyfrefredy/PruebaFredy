import { ChecklistContainer } from "./checklist-container";


describe('ChecklistContainer', () => {
  it('should create an instance', () => {
    expect(new ChecklistContainer()).toBeTruthy();
  });
});
