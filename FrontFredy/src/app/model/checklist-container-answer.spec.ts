import { ChecklistContainerAnswer } from "./checklist-container-answer";


describe('ChecklistContainerAnswer', () => {
  it('should create an instance', () => {
    expect(new ChecklistContainerAnswer()).toBeTruthy();
  });
});
